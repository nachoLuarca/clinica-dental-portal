VITE_API_BASE_URL=http://127.0.0.1:8081/api

# ASUNCION TEMPORAL: aun no esta definido si en produccion el tenant se
# resuelve por subdominio o por otra via. Mientras tanto, cada instancia de
# este sitio apunta a una sola clinica via variable de entorno. Ajustar
# cuando se defina la estrategia de multi-tenant para el sitio publico.
VITE_CLINICA_SLUG=clinica-demo
